<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="nav mb-10">
            <a class="text-blue-500" href="/">Back to Home</a>
        </div>
        <h1 class="text-3xl font-bold mb-6">All Users</h1>

        <% if(users.length > 0) { %>
            <div class="users flex gap-3 flex-wrap">
                <% users.forEach(function(user) { %>
                    <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
                        <div class="w-full h-72 bg-zinc-700 rounded-lg overflow-hidden">
                            <img class="w-full h-full object-top" src="<%= user.image %>" alt="<%= user.name %>">
                        </div>
                        <h3 class="text-xl tracking-tighter mt-3"><%= user.name %></h3>
                        <h3 class="text-zinc-500 overflow-hidden"><%= user.email %></h3>
                        <div class="flex gap-3">
                            <a class="text-zinc-200 mt-4" href="/edit/<%= user._id %>">Edit User</a>
                            <a class="text-red-500 ml-7 mt-4" href="/delete/<%= user._id %>">Delete User</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <h2 class="text-2xl font-bold">No Users Found</h2>
        <% } %>
    </div>
</body>
</html>

