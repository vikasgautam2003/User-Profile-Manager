<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
     <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="nav mb-10">
            <a class="text-blue-500" href="/read">Listed Users</a>
        </div>
        <h1 class="text-3xl font-bold mb-6">Update User</h1>
    <form action="/update/<%= user._id %>" method="POST" >
      <input 
        type="text" 
        name="name" 
        placeholder="Enter username" 
        value = "<%= user.name %>"
        class="px-10 py-3 border-2 outline-none border-zinc-800 rounded-lg bg-transparent"
      />

      <input 
        type="text" 
        name="email" 
        placeholder="Enter Email" 
        value = "<%= user.email %>"
        class="px-10 py-3 border-2 outline-none border-zinc-800 rounded-lg bg-transparent"
      />

      <input 
        type="text" 
        name="image" 
        placeholder="Enter Image URl" 
        value = "<%= user.image %>"
        class="px-10 py-3 border-2 outline-none border-zinc-800 rounded-lg bg-transparent"
      />

      <input class="px-5 py-2 bg-blue-500 rounded-lg ml-3" type="submit" value="Update User">

    </form>
  </div>
</body>
</html>